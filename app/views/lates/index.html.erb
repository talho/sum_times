<h1>Listing lates</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lates.each do |late| %>
  <tr>
    <td><%= link_to 'Show', late %></td>
    <td><%= link_to 'Edit', edit_late_path(late) %></td>
    <td><%= link_to 'Destroy', late, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Late', new_late_path %>
