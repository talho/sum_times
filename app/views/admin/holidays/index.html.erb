<h1>Listing admin_holidays</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_holidays.each do |admin_holiday| %>
  <tr>
    <td><%= link_to 'Show', admin_holiday %></td>
    <td><%= link_to 'Edit', edit_admin_holiday_path(admin_holiday) %></td>
    <td><%= link_to 'Destroy', admin_holiday, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Holiday', new_admin_holiday_path %>
