<%= form_for(@admin_supervisor) do |f| %>
  <% if @admin_supervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_supervisor.errors.count, "error") %> prohibited this admin_supervisor from being saved:</h2>

      <ul>
      <% @admin_supervisor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
