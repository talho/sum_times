<h1>Listing admin_supervisors</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_supervisors.each do |admin_supervisor| %>
  <tr>
    <td><%= link_to 'Show', admin_supervisor %></td>
    <td><%= link_to 'Edit', edit_admin_supervisor_path(admin_supervisor) %></td>
    <td><%= link_to 'Destroy', admin_supervisor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Supervisor', new_admin_supervisor_path %>
