<h2>New schedule</h2>

<%= form_for @schedule, :url => admin_schedules_path do |f| %>
  <%= render :partial => '/schedules/form', :locals => {f: f} %>
<% end %>

<%= link_to 'Back', admin_profile_path(@schedule.user_id) %>
